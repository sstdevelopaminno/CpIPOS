import { requireItSupport } from "@/lib/it-admin-guard";
export const dynamic="force-dynamic";
export default async function AuditLogsPage(){const {supabase}=await requireItSupport();const {data,error}=await supabase.from("audit_logs").select("id,action,actor_role,target_table,target_id,created_at,tenant_id,branch_id").order("created_at",{ascending:false}).limit(100);if(error)throw new Error(error.message);return <section className="surface"><h2>รายงานตรวจสอบระบบ</h2><p>100 เหตุการณ์ล่าสุดจาก immutable audit trail</p><div style={{overflowX:"auto"}}><table style={{width:"100%",borderCollapse:"collapse",fontSize:11}}><thead><tr style={{textAlign:"left",color:"#718096"}}><th style={{padding:10}}>เวลา</th><th>Action</th><th>Role</th><th>Target</th><th>Tenant / Branch</th></tr></thead><tbody>{(data??[]).map(row=><tr key={row.id} style={{borderTop:"1px solid #e8edf3"}}><td style={{padding:12}}>{new Date(row.created_at).toLocaleString("th-TH")}</td><td><strong>{row.action}</strong></td><td>{row.actor_role??"-"}</td><td>{row.target_table??"-"} {row.target_id?`· ${String(row.target_id).slice(0,8)}`:""}</td><td>{row.tenant_id?String(row.tenant_id).slice(0,8):"-"} / {row.branch_id?String(row.branch_id).slice(0,8):"-"}</td></tr>)}</tbody></table></div></section>}
